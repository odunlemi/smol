PORT=3000
BASE_URL=http://localhost:3000
DB_PATH=./smol.db

# Rate limiting
RATE_LIMIT_MAX=5
RATE_LIMIT_WINDOW_MS=3600000

# Link expiry (6 months in milliseconds)
LINK_TTL_MS=15552000000

# Buffer flush interval (ms). Batches DB writes to handle traffic spikes
WRITE_BUFFER_FLUSH_MS=300